I did not fix any of the design pattern i finished the implementation of the required functionality of the project.

The main thing i changed is how ID is generated, where ID uses the currentTime %1000 to the end of the name rather then a increment value